{
	"name": "daily-note-lookback",
	"version": "1.0.0",
	"description": "Obsidian plugin: render lazy lookbacks of any daily-note section from previous years, months, or custom intervals — collapsed until expanded, far lighter than native embeds.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"test": "node test/run.mjs"
	},
	"keywords": ["obsidian", "obsidian-plugin", "daily-notes", "journal", "lookback"],
	"author": "Dyllon Wright",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.14.0",
		"esbuild": "^0.24.0",
		"obsidian": "latest",
		"typescript": "^5.6.0"
	}
}
